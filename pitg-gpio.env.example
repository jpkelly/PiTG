# Copy to /etc/default/pitg-gpio to override defaults.
# -T enables test mode: PerfectCue auto-fires every 10s (next/prev alternating)
# Remove -T for live show operation.

PITG_GPIO_OPTS="-p limitimer -u /dev/ttyLimitimer -b 19200 -i 250 -c 0 -t 10:00 -T"
